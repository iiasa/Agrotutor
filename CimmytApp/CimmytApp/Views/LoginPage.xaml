<?xml version="1.0" encoding="utf-8" ?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:localization="clr-namespace:Helper.Localization.Localization;assembly=Helper.Localization"
    prism:ViewModelLocator.AutowireViewModel="True" x:Class="CimmytApp.Views.LoginPage"
    Title="{localization:Translate page_title_login}">

    <StackLayout>
        <Entry Text="{Binding Email}" Placeholder="{localization:Translate placeholder_email}" />
        <Entry IsPassword="True" Text="{Binding Password}" Placeholder="{localization:Translate placeholder_password}" />
        <Button Text="{localization:Translate btn_login_text}" Command="{Binding LoginCommand}" />
        <Label Text="{localization:Translate hint_register}" />
        <Button Text="{localization:Translate btn_go_to_registration_text}"
            Command="{Binding NavigateToRegistrationCommand}" />
    </StackLayout>
</ContentPage>