<?xml version="1.0" encoding="utf-8" ?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:i18n="clr-namespace:CimmytApp.Core.Localization;assembly=CimmytApp"
    prism:ViewModelLocator.AutowireViewModel="True" x:Class="CimmytApp.Views.ProfilePage" Padding="16">
    <ScrollView>
        <StackLayout Orientation="Vertical">

            <StackLayout Orientation="Horizontal" HorizontalOptions="End">
                <Label Text="Perfil de productor" FontAttributes="Italic" />
                <Image Source="profile_small.png" />
            </StackLayout>

            <Label Text="{i18n:Translate hint_producer_name}" FontSize="10" />
            <Entry Placeholder="{i18n:Translate hint_firstname_lastname}" Text="{Binding UserProfile.UserName}"
                Margin="0,0,0,16" />

            <Label Text="Estado" FontSize="10" />
            <Picker ItemsSource="{Binding States}" SelectedIndex="{Binding StatesSelectedIndex}" Title="seleccionar"
                Margin="0,0,0,16" />

            <Button Text="Tomar una foto" Command="{Binding TakePictureCommand}" />
            <Image IsVisible="{Binding PictureVisible}" HorizontalOptions="Center" WidthRequest="350"
                Source="{Binding ImageSource}" />
        </StackLayout>
    </ScrollView>
</ContentPage>